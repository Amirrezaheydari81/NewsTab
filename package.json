{
  "name": "news-tab",
  "version": "1.1.0.0",
  "description": "Minimal RSS new tab extension",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "test": "node test.js"
  },
  "devDependencies": {
    "terser": "^5.39.0",
    "jsdom": "^25.0.0"
  }
}
